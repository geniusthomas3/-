#include <stdio.h>
#include <stdlib.h>

/*int gcd(int,int);

int main()
{
    int a,b;
    scanf("%d %d",&a,&b);
    int cd=gcd(a,b);
    printf("%d\n%d",cd,a*b/cd);
    return 0;
}

int gcd(int a,int b)
{
    if(b==0)
    {
        return a;
    }
    else
    {
        return gcd(b,a%b);
    }
}
*/
int main()
{
    ///기준 list+0;
    char list[100][10];
    char *cmp;
    char
    int k;
    scanf("%d",&k);
    for(int i=0;i<k;i++)
    {
        scanf("%s",list+i);
    }
    int flag=0
    for(int i=1;i<k;i++)
    {
        cmp=list+i;
        for(int j=0;;j++)
        {


        }
    }

}

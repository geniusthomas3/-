#include <stdio.h>
#include <vector>

using namespace std;

int n;
int arr[100000][2];

vector<>

int main()
{
    freopen("triangles.in","r",stdin);
    freopen("triangles.out","w",stdout);
    scanf("%d",&n);
    for(int i=0;i<n;i++){
        scanf("%d %d",&arr[i][0],&arr[i][1]);
    }
}

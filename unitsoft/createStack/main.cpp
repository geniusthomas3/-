#include <stdio.h>

int main(){
    char str[100];
    int n,lim,sta[100],point=0;
    scanf("%d",&n);
    for(int i=0;i<n;i++){
        scanf("%s",str)
        if(s=="create"){
            scanf("%d",&lim);
        }
        if(s=="pop"){

        }
    }
}

#include <stdio.h>
#include <queue>

int main(){
    int n,l;
    long long int arr[5000000];
    scanf("%d %d",&l,&h);
    for(int i=0;i<n;i++){
        scanf("%lld",arr+i);
    }
    for(int i=0;i<)
}

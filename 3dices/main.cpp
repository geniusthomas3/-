#include <stdio.h>

int main()
{
    int a[3],num=0,state;
    FILE *input;
    FILE *output;
    input=fopen("INPUT.TXT","r");
    output=fopen("OUTPUT.TXT","w");
    fscanf(input,"%d %d %d",a,a+1,a+2);
    if(a[0]==a[1])
    {

    }
    else()
    switch()
}
